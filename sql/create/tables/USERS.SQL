CREATE TABLE USERS (
   USERNAME VARCHAR2(50),
   USER_REAL_NAME VARCHAR2(100),
   PASSWD VARCHAR2(255),
   ENABLED CHAR(1),
   EMAIL VARCHAR2(255),
   EMAIL_SHOW CHAR(1),
   DESCRIPTION VARCHAR2(255),
   ID NUMBER PRIMARY KEY,
   VERSION NUMBER

);